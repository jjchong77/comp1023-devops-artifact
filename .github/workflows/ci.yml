on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  run-script:
    runs-on: windows-latest
    steps: 
      - name: checkout
        uses: actions/checkout@v4
      - name: run
        run: scripts/check_artifact.cmd
